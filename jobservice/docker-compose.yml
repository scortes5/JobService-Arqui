version: "3.9"

services:
  jobmaster:
    build:
      context: .              # 👈 todo jobservice como contexto
      dockerfile: jobmaster/Dockerfile
    env_file:
      - .env
    ports:
      - "4000:4000"
    depends_on:
      - redis

  worker1:
    build:
      context: .              # 👈 mismo contexto, ve services/ también
      dockerfile: worker/Dockerfile
    env_file:
      - .env
    depends_on:
      - redis

  worker2:
    build:
      context: .
      dockerfile: worker/Dockerfile
    env_file:
      - .env
    depends_on:
      - redis

  worker3:
    build:
      context: .
      dockerfile: worker/Dockerfile
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
